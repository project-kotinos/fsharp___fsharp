
build_targets:
  - name: latest
    commands:
     - bash ./install-mono.sh
     - bash ./mono/build-mono.sh 
     - bash ./mono/test-mono.sh

ci:
  builds:
    - name: latest
      build_target: latest
